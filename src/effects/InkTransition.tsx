import type { ReactNode } from "react";

export function InkTransition({
  enabled = false,
  children,
}: {
  enabled?: boolean;
  children: ReactNode;
}) {
  // TODO: Implement Ink Transition V1
  return <>{children}</>;
}
