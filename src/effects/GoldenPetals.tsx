import type { ReactNode } from "react";

export function GoldenPetals({
  enabled = false,
  children,
}: {
  enabled?: boolean;
  children: ReactNode;
}) {
  // TODO: Implement Golden Petals V1
  return <>{children}</>;
}
