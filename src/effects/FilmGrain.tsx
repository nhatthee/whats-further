import type { ReactNode } from "react";

export function FilmGrain({
  enabled = false,
  children,
}: {
  enabled?: boolean;
  children: ReactNode;
}) {
  // TODO: Implement Film Grain V1
  return <>{children}</>;
}
