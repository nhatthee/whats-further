import type { ReactNode } from "react";

export function LightSweep({
  enabled = false,
  children,
}: {
  enabled?: boolean;
  children: ReactNode;
}) {
  // TODO: Implement Light Sweep V1
  return <>{children}</>;
}
